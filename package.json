{
  "name": "tiny-eventsource",
  "version": "2.0.0",
  "description": "Tiny EventSource for API servers",
  "source": "src/eventstream.js",
  "main": "dist/tiny-eventstream.cjs",
  "module": "dist/tiny-eventstream.esm.js",
  "type": "module",
  "sourceType": "module",
  "types": "types/eventstream.d.ts",
  "engines": {
    "node": ">=16"
  },
  "engineStrict": true,
  "scripts": {
    "build": "npm run lint && rm -rf dist/* && npm run rollup && npm run mocha",
    "changelog": "auto-changelog -p",
    "lint": "eslint *.js src/*.js test/*.js",
    "mocha": "mocha test/*.js",
    "rollup": "rollup --config",
    "test": "npm run lint && npm run mocha",
    "types": "npx -p typescript tsc src/*.js --declaration --allowJs --emitDeclarationOnly --outDir ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/avoidwork/tiny-eventsource.git"
  },
  "keywords": [
    "eventsource",
    "sse",
    "push"
  ],
  "files": [
    "dist",
    "types"
  ],
  "author": "Jason Mulligan <jason.mulligan@avoidwork.com>",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/avoidwork/tiny-eventsource/issues"
  },
  "homepage": "https://github.com/avoidwork/tiny-eventsource#readme",
  "dependencies": {},
  "devDependencies": {
    "auto-changelog": "^2.4.0",
    "eslint": "^8.23.1",
    "mocha": "^10.0.0",
    "rollup": "^2.79.0",
    "rollup-plugin-terser": "^7.0.2",
    "typescript": "^4.8.3"
  }
}
